import 'package:flutter/material.dart';

class AstrologyMoon extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Moon"),
      ),
    );
  }
}
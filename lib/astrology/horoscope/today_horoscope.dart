import 'package:flutter/material.dart';

class TodayHoroscope extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Today"),
      ),
    );
  }
}
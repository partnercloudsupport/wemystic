import 'package:flutter/material.dart';

class DetailHoroscope extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return new Container(


      child: Text("Year"),
    );
  }
}
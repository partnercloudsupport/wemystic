import 'package:flutter/material.dart';

class AstrologyBirthChart extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Birth Chart"),
      ),
    );
  }
}
name: wemystic
description: A new Flutter application.

dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^0.1.2
  firebase_auth: any
  google_sign_in: any
  shared_preferences: any
  flutter_secure_storage: any
  flutter_facebook_login: any
  package_info: any
  cloud_firestore: any
  firebase_analytics:
  flutter_date_picker: "^0.1.2"
  intl: any
  flutter_webview_plugin: any
  flutter_html_view: any
  flutter_cupertino_date_picker: ^0.0.6

dependency_overrides:
  cached_network_image: ^0.4.0
  http: any
  flutter_cache_manager: ^0.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter


  uuid: ^1.0.3
# For information on the generic Dart part of this file, see the
# following page: https://www.dartlang.org/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:
  assets:
    - images/zodiac/aquario.png
    - images/zodiac/aries.png
    - images/zodiac/capricornio.png
    - images/zodiac/caranguejo.png
    - images/zodiac/escorpiao.png
    - images/zodiac/gemeos.png
    - images/zodiac/leao.png
    - images/zodiac/libra.png
    - images/zodiac/peixes.png
    - images/zodiac/sagitario.png
    - images/zodiac/touro.png
    - images/zodiac/virgem.png
  uses-material-design: true

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.io/assets-and-images/#resolution-aware.

  # For details regarding adding assets from package dependencies, see
  # https://flutter.io/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.io/custom-fonts/#from-packages
